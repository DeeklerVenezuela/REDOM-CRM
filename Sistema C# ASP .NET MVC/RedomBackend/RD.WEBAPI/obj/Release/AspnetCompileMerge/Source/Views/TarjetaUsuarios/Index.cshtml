@model IEnumerable<RD.Data.Models.TarjetaUsuario>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FechaVencimiento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NumeroTarjeta)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FechaVencimiento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NumeroTarjeta)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TarjetaUsuarioID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TarjetaUsuarioID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TarjetaUsuarioID })
        </td>
    </tr>
}

</table>
